import ServidoresBackend from '../../../Synapse/ServidoresBackend';

export const metadata = { title: 'Backserver | NativeCode Finance' };

export default function BackserverPage() {
    return <ServidoresBackend />;
}
